name: hello-world
description: "Daily heartbeat logger — verifies CronJob execution and cluster health"

build:
  enabled: true
  dockerfile: ./Dockerfile
  context: ./

image:
  registry: ghcr.io/rprakashdass
  name: hello-world

deploy:
  enabled: true
  chart: ./Chart.yaml
  helmChart: true

versioning:
  strategy: git-sha
  autoUpdate: true

domain: automation

dependencies: []
