apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-hello-world
  namespace: argocd
  labels:
    app.kubernetes.io/name: home-hello-world
    app.kubernetes.io/component: argocd-application
    app.kubernetes.io/part-of: home-automation-platform
  annotations:
    app.kubernetes.io/description: "Daily hello-world logger"
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: home

  source:
    repoURL: https://github.com/rprakashdass/homelab-gitops.git
    targetRevision: main
    path: charts/hello-world

  destination:
    server: https://kubernetes.default.svc
    namespace: home

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
